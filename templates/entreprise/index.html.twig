{% extends 'base.html.twig' %}

{% block title %}Hello EntrepriseController!{% endblock %}

{% block body %}
<a href="{{ path('add_entreprise') }}">Ajouter une entreprise</a>
<h1> Liste des entreprises </h1>
    {%for entreprise in entreprises %}
        <a href="{{ path('show_entreprise', {'id':entreprise.id}) }}">{{entreprise}}</a> 
        <a href ="{{ path('edit_entreprise', {'id':entreprise.id}) }}">Edit</a><br>
        <a href ="{{ path('delete_entreprise', {'id':entreprise.id}) }}">Delete</a><br>
    {% endfor %}
    
{% endblock %}
